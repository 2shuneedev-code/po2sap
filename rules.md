# 업무 규칙 설명서 (현업용) — ❄ 폐기 · 자동 생성 예정

> **이 문서는 참고 대상이 아니다. 개발자는 읽지 말 것.**
> 2026-09-11 · 내용 폐기 (거래처 자동판별 · MSC=PDF · KL=HTM 등 사실과 다른 서술 포함)

---

## 왜 비워두는가

이 문서는 **손으로 쓰면 안 되는 문서다.**

거래처 규칙의 원본은 `masters/customers/*.yaml` 이다.
사람이 설명서를 따로 쓰면 규칙을 바꿀 때마다 두 곳을 고쳐야 하고,
한 곳만 고치는 순간 **문서와 실동작이 어긋나 오더가 잘못 생성된다.**

실제로 폐기 전 이 문서에는 아래 오류가 있었다.

| 잘못된 서술 | 사실 |
|---|---|
| MSC = PDF, KL = HTM | **MSC = HTM, KL = PDF** (정반대) |
| "어느 거래처인지 판단" | 자동판별 안 한다. **사용자가 먼저 선택** |
| "중복 오더가 생기지 않게 막습니다" | 멱등키 없음. **중복 전송은 SAP 에서 덮어씀** |
| "전송 이력에서 확인 가능" | 이력 화면 **범위 제외** |

→ 방향을 뒤집는다: `설명서 → 규칙`(위험) 이 아니라 **`규칙 → 설명서`(안전)**.

## 그럼 현업은 무엇을 보는가

| 시점 | 보는 것 | 원천 |
|---|---|---|
| 업로드 직전 | 화면의 **규칙 미리보기 카드** | `GET /api/masters/customers/{code}/preview` ← YAML 자동 생성 |
| 검수 중 | 셀 색상(🔴🟡🔵) + 하단 이슈 목록 | 검증 결과 |
| 값 근거 확인 | 셀 hover 툴팁 (2차) | 규칙 경로 + 원문 위치 |

화면이 곧 설명서다. 화면 정의는 **`process.md` §4**.

## 이 파일의 미래

**D6 (마스터 관리 화면)** 에서 이 파일을 **YAML 로부터 자동 생성**한다.
설계는 `master-admin.md` §M3 (현재 보류).
그때까지 이 파일은 비어 있는 것이 정상이다.
