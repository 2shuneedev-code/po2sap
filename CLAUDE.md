# PO2SAP — 작업 지침 (오케스트레이터용)

> 이 파일은 **메인 세션이 항상 읽는 프로젝트 규칙**이다.
> 에이전트 호출 순서와 금지 사항을 여기서 통제한다.
> v1.0 · 2026-09-11

---

## 1. 이 프로젝트가 무엇인가

거래처 발주서(PDF/HTM) → Claude 추출 → **규칙엔진** → 스프레드시트 검수 → EAI(JSON) 전송 → SAP CBO 적재.

**절대 원칙 하나**: Claude 는 원문을 **읽기만** 하고, SAP 코드(브랜드·출하처·비고)는
`masters/` 의 규칙이 **결정적으로** 정한다. 이걸 섞으면 SAP 오더가 잘못 생성된다.

---

## 2. 문서 우선순위 (이것만 신뢰한다)

| 순위 | 파일 | 용도 |
|---|---|---|
| 1 | `NEXT.md` | 현재 상태 · 확정된 결정 · 다음 할 일 |
| 2 | `masters/SCHEMA.md` | **백엔드 구현 명세** (규칙 스키마 · 엔진 7단계) |
| 3 | `contracts/api-contract.md` | **프론트 구현 명세** (API 계약) |
| 4 | `design.md` | 기술 스택 · 아키텍처 · 디렉토리 · 배포 |
| 5 | `process.md` | 업무 흐름 · 화면 정의 |
| 6 | `masters/_base/sap_defaults.yaml`<br>`masters/customers/*.yaml` | **값의 원천** (필드 목록 · 거래처 규칙) |

### 🚫 읽지 말 것

| 파일 | 이유 |
|---|---|
| `rules.md` | **폐기됨.** 내용이 사실과 반대였다(MSC/KL 파일형식). D6에 자동 생성 예정 |
| `master-admin.md` | D6 보류. 현재 구현 대상 아님 |
| `.claude/agents/architect.md` 의 "필수 반영 규칙 스펙" | **초기 브리프.** 33필드·거래처 4곳 등 낡음. 규칙의 원천은 `masters/customers/*.yaml` |

> 문서끼리 모순되면 **위 표의 순위가 높은 쪽**과 `masters/` 가 이긴다.
> 발견 즉시 사용자에게 보고한다. 임의로 낮은 쪽에 맞추지 않는다.

---

## 3. 에이전트 실행 루프 ★

**서브에이전트는 다른 서브에이전트를 호출할 수 없다.** (Task 도구가 없음)
따라서 **연결은 메인 세션(오케스트레이터)이 책임진다.** 아래 순서를 자동으로 수행한다.

```
        ┌──────────────────────────────────────────────┐
        │  ① architect   설계·문서 (코드 안 씀)         │
        └───────────────────┬──────────────────────────┘
                            ▼
        ┌──────────────────────────────────────────────┐
        │  ② developer   구현 (한 번에 작업 1개)        │
        │     → 끝나면 "테스트 필요 지점"을 보고        │
        └───────────────────┬──────────────────────────┘
                            ▼   ★ 자동 호출
        ┌──────────────────────────────────────────────┐
        │  ③ tester      독립 검증 (코드 수정 금지)      │
        └───────────────────┬──────────────────────────┘
                   실패 ┌───┴───┐ 통과
                        ▼       ▼
                  ②로 복귀   ┌──────────────────────────┐
                  (최대 3회) │  ④ reviewer  품질·보안    │
                             └───────────┬──────────────┘
                                 critical│      없음
                                    ②로  │      ▼
                                          커밋 → 사용자 보고
```

### 오케스트레이터 규칙

| # | 규칙 |
|---|---|
| R1 | developer 가 **코드를 쓴 직후 tester 를 반드시 호출한다.** 사용자가 시키지 않아도 자동으로 |
| R2 | tester 가 실패를 보고하면 **그 내용을 그대로 developer 에게 전달**해 수정시킨다 |
| R3 | ②↔③ 왕복은 **최대 3회.** 3회로 못 고치면 멈추고 사용자에게 원인을 보고한다 (무한루프 금지) |
| R4 | tester 통과 후 **reviewer 를 호출한다.** `critical` 이 있으면 ②로 되돌린다 |
| R5 | 설계 변경이 필요하다고 판단되면 developer 를 멈추고 **architect 를 호출**한다. 코드가 설계를 앞서가지 않게 한다 |
| R6 | 작업 1개 = 커밋 1개. 통과한 단위는 **즉시 커밋**한다 (§5) |

---

## 4. 브랜치 전략 (확정: B안)

```
main        항상 동작하는 상태. reviewer 통과분만 들어온다
└── developer   ★ 모든 구현 작업이 여기서 일어난다
```

| 규칙 | 내용 |
|---|---|
| 작업 브랜치 | **`developer`** 에서 작업하고 push 한다 |
| main 반영 | 하나의 마일스톤(D2, D3 …)이 끝나고 reviewer 통과 시 `main` 으로 머지 |
| 머지 방식 | `git checkout main && git merge --ff-only developer && git push` |
| 규모가 커지면 | 그때 `feat/*` 를 파생한다. **지금은 단일 브랜치로 단순하게 간다** |

> ⚠ **GitHub Desktop 주의**: 브랜치 전환 시 변경사항을 **조용히 stash** 한다.
> 파일이 사라졌으면 삭제된 게 아니니 `git stash list` 부터 확인할 것.
> (2026-09-11 에 문서 정리본이 이렇게 사라진 적이 있다)

---

## 5. 커밋 · 보안

```
feat:  기능      fix:   버그      rules: 거래처 규칙(YAML) 변경
docs:  문서      chore: 설정      test:  테스트
```

| 수칙 | 명령 / 내용 |
|---|---|
| **push 전 대외비 확인** | `git ls-files samples/` → **2개**(`.gitignore`, `README.md`) 가 아니면 **중단** |
| `samples/` | 실물 발주서·단가 = 대외비. **절대 커밋 금지.** 테스트용은 마스킹 후 `backend/tests/fixtures/` |
| `.env` | 커밋 금지. 변경은 `.env.example` 에만 |
| 작업 후 | **즉시 커밋.** GUI 의 "Discard/변경취소" 는 복구 불가 |
| `rules:` 커밋 | 본문에 **근거와 요청자**를 남긴다. Git 이력이 곧 규칙 변경 대장 |

---

## 6. 구현 시 절대 금지 ★

| # | 금지 | 이유 |
|---|---|---|
| G1 | **거래처 이름을 코드에 쓰는 것** (`if customer == "MSC"`) | 거래처 50곳 확장 시 if문 지옥. 규칙은 전부 YAML |
| G2 | **필드 개수를 하드코딩** (`36`, 33개 리스트 등) | 필드는 `_base/sap_defaults.yaml` 이 유일한 원천. 34개가 되어도 코드는 그대로 동작해야 한다 |
| G3 | **LLM 에게 SAP 코드를 정하게 하는 것** | 재현성·감사 붕괴. LLM 은 원문 읽기만 |
| G4 | **값이 미정이라고 개발을 멈추는 것** | YAML 에 `todo:` 달고 진행한다 |
| G5 | 전송 페이로드에 `null` 사용 | 빈 값은 `""`. 숫자·날짜도 전부 문자열 |
| G6 | 실물 발주서를 픽스처로 커밋 | 마스킹 필수 |

---

## 7. 지금 할 일

`NEXT.md §3` 을 따른다. 현재는 **D2 규칙엔진**(백엔드) 과 **D3 프론트 그리드** 가 다음 작업이다.
