# 마스터 관리 화면 설계서

> 거래처 규칙(브랜드·도착지·고정값·참조표)을 **화면에서 직접 수정**하고,
> 저장 즉시 파싱에 반영하며, 현업용 설명서(`rules.md`)를 자동 생성한다.
>
> 버전 **v1.0** · 2026-09-10 · 상위 문서 `design.md` §5
> 대상 마일스톤: **D6** (D1~D5 이후)

---

## 0. 결정 요약

| # | 결정 | 근거 |
|---|---|---|
| M1 | **저장 원본은 YAML 유지.** MD를 원본으로 쓰지 않는다 | MD는 자유 서식 → 문법 검증 불가 → 규칙이 조용히 깨지면 **SAP 오더가 잘못 생성됨** |
| M2 | **사용자는 YAML을 보지 않는다** | 화면은 전부 표/폼. 원문 편집기는 관리자 전용 고급 모드로만 노출 |
| M3 | **MD는 출력물** — 저장 시 `rules.md` 자동 재생성 | 문서-실동작 불일치 원천 차단 (`design.md` P3와 동일 사상) |
| M4 | 저장 전 **스키마 검증 + 영향 미리보기** 필수 | 잘못된 규칙의 운영 반영 차단 |
| M5 | 저장 시 **이전 버전 스냅샷 백업 + 1클릭 롤백** | DB 없이도 변경 이력 확보 |
| M6 | DB 도입 **보류** — 파일 + 스냅샷으로 충분 | `design.md` D8 유지 |

### M1 보충 — "MD를 원본으로" 안 하는 이유

| 위험 | 결과 |
|---|---|
| MD는 문법 검사가 불가능 | 표 구분자(`\|`) 하나 누락 → 규칙 전체가 조용히 깨짐 |
| 규칙과 설명 문장이 섞임 | 프로그램이 "규칙"과 "메모"를 구분할 수 없음 |
| MD → 규칙 역파서가 별도로 필요 | 발주서 파싱과 맞먹는 난이도의 코드가 하나 더 생김 |
| 중첩 구조 표현 불가 | `ZPKRE2 = 기본값 + 참조표 B코드 + C코드` 같은 결합 규칙을 표 한 줄로 못 씀 |

> **방향을 뒤집는다**: `MD → 규칙`(위험) 이 아니라 `규칙 → MD`(안전).

---

## 1. 데이터 흐름

```
 [마스터 관리 화면]                    [서버]                      [파일]
      표 편집
        │
        ├─(1) 실시간 검증 요청 ───────► 스키마 검증 ──► 오류 인라인 표시
        │
        ├─(2) [미리보기] ────────────► 샘플 발주서 재실행
        │                              현재규칙 vs 변경규칙 diff
        │     ◄── 전/후 비교표 ────────┘
        │
        └─(3) [저장] ────────────────► ① 스키마 검증 (실패 시 거부)
                                       ② 이전 버전 스냅샷 백업 ──► masters/_history/
                                       ③ YAML 원자적 쓰기      ──► masters/customers/msc.yaml
                                       ④ 마스터 캐시 무효화
                                       ⑤ rules.md 자동 재생성  ──► rules.md
                                       ⑥ 변경 감사 로그        ──► storage/audit/masters/
                                            │
                                            ▼
                                   다음 파싱부터 즉시 반영 (재시작 불필요)
```

> 로더는 이미 `mtime`을 캐시 키에 포함하므로(`masters/loader.py`) 파일이 바뀌면 자동 재로딩된다.
> 다만 저장 직후 **명시적 캐시 무효화**를 호출해 경합을 없앤다.

---

## 2. 화면 구성

### 2.1 목록 화면 `/admin/masters`

| 컬럼 | 내용 |
|---|---|
| 거래처 | MSC / KL / YGJP |
| 고객코드 | 100249 / 107525 / 3200 |
| 파일형식 | PDF / HTM |
| 상태 | active / draft |
| 책임자(owner) | 규칙 변경 승인자 |
| 최종수정 | 일시 + 수정자 |

`[+ 신규 거래처]` → 템플릿(`_base/sap_defaults` 상속)으로 초안 생성 → `status: draft`

### 2.2 편집 화면 `/admin/masters/{code}`

좌측 탭 / 우측 편집 영역.

| 탭 | 편집 대상 (YAML 경로) | UI |
|---|---|---|
| **기본정보** | `meta` | 폼 (거래처명·고객코드·파일형식·책임자·상태) |
| **파싱 힌트** | `extraction.hints` | 여러 줄 텍스트. "Claude에게 주는 문서 설명"이라고 안내 |
| **판별 규칙** | `rules.*` (keyword_map / exact_map / lookup) | 표 편집 (§2.3) |
| **분기표** | `tables.*` (decision_table) | 표 편집 (§2.4) |
| **SAP 33필드** | `fields.*` | 33행 고정 표 (§2.5) |
| **화면 컬럼** | `grid` | 체크박스(숨김) + 드래그(순서) |
| **참조표** | `refs/*.csv` | CSV 그리드 + 업로드/내보내기 (§2.6) |
| **변경 이력** | `_history/` | 목록 + diff + 되돌리기 |

상단 고정 바: `[미리보기]` `[저장]` `[취소]` · 미저장 변경 시 이탈 경고.

### 2.3 판별 규칙 편집 (keyword_map / exact_map)

```
브랜드 판별 (ZBRAND)                        종류: 키워드 포함
설명: 발주서 문구에 아래 단어가 있으면 해당 코드 적용
위에서부터 순서대로 확인합니다.   ← 순서가 의미를 가짐을 명시

  ⣿  발주서 문구        SAP 코드              
  ⣿  HERTEL             38          [삭제]
  ⣿  INTERSTATE         127         [삭제]
  ⣿  ACCUPRO            205         [삭제]
  ⣿  CLASS C            428         [삭제]
      [+ 행 추가]

  못 찾았을 때:  ( ) 그냥 진행   ( ) 경고(노랑)   (•) 오류(빨강, 전송 차단)
  메시지: [MSC 브랜드 키워드를 찾을 수 없습니다               ]
```

- `⣿` 드래그로 **순서 변경** (KL의 `WIDIA GTD` 우선 같은 케이스가 순서에 의존)
- 순서 의존 규칙에는 **"위에서부터 첫 매칭"** 안내 배너 상시 노출
- YGJP 24종처럼 항목이 많으면 검색 + CSV 붙여넣기 지원

### 2.4 분기표 편집 (decision_table)

```
출하처(Ship To) 분기                        평가 단위: 품목별(line)

  조건: Ship To 주소에 포함  →  결정: KUNNR2(도착지), _pack_base(포장구분 기본값)

  ⣿  포함 문구      KUNNR2     포장구분    메모
  ⣿  ELKHART        100249     C           인디애나 물류센터   [삭제]
  ⣿  HARRISBURG     319677     N                               [삭제]
  ⣿  RENO           319678     O                               [삭제]
  ⣿  ATLANTA        319679     A                               [삭제]
      [+ 행 추가]

  ⚠ 기본행(*)이 없습니다. 어느 행에도 안 걸리면 → 오류(빨강) 처리됩니다.
```

**저장 시 자동 점검** (`design.md` §5.3 CI 항목을 화면으로 끌어올림)
- 도달 불가 행 (위 행에 흡수되어 절대 안 걸리는 행)
- 중복 조건
- 기본행(`*`) 누락 → 경고 (의도적이면 무시 가능)

### 2.5 SAP 33필드 매핑

33행 **고정** 표. 행 추가/삭제 불가 — 필드 누락을 구조적으로 차단(`design.md` D4).

| 필드 | 한글명 | 값의 출처 | 설정 |
|---|---|---|---|
| AUART | 오더유형 | `고정값` ▾ | `ZOR` |
| KUNNR2 | 도착지 | `분기표` ▾ | `ship_to_routing` ▾ |
| ZBRAND | 브랜드 | `판별규칙` ▾ | `brand_code` ▾ |
| BSTKD | 고객발주번호 | `발주서에서` ▾ | `header.po_number` ▾ · ☑필수 |
| ZPKRE2 | 포장구분2 | `수식` ▾ | 🔒 (§2.5.1) |
| POSEX | 품목번호 | `자동생성` ▾ | `행번호×10` ▾ |

- 출처 6종: `고정값(const)` `발주서에서(doc)` `분기표(table)` `판별규칙(rule)` `수식(expr)` `자동생성(gen)`
- 드롭다운은 **해당 거래처에 실제 정의된 규칙/분기표만** 표시 → 오타 불가
- 길이 제한(`_base/sap_defaults.yaml`의 `max_len`)을 입력란에 실시간 표시

#### 2.5.1 수식(expr)은 **읽기 전용**

`ZPKRE2`의 `join(",", compact([...]))` 같은 수식은 화면에서 **설명문으로만** 보여준다.

```
ZPKRE2  포장구분2   [수식 🔒]
  "출하처 기본값(C/N/O/A) 뒤에 참조표의 B코드·C코드를 쉼표로 이어 붙입니다"
  예시:  N  +  B12  +  C7   →   N,B12,C7
  ⓘ 수식 변경은 시스템 담당자에게 요청하세요.
```

> **근거**: 수식은 문법 오류 시 전 행이 깨진다. 현업이 자유 편집할 영역이 아니다.
> `design.md` §5.2 — "`expr`이 늘어나면 설계 실패 신호". 수식을 못 늘리게 막는 것이 곧 설계 방어선.

### 2.6 참조표(CSV) 편집

MSC B/C코드 참조표(`refs/msc_ref.csv`). 현업 요청 1순위 항목.

- 그리드 직접 편집 + 검색
- **엑셀 붙여넣기** / CSV 업로드(전체 교체, 교체 전 diff 확인) / 내보내기
- 키(`our_item`) 중복 검사
- 행 수가 많아지면 서버 페이징 (수만 행 대비)

---

## 3. 안전장치

### 3.1 저장 전 검증 (실패 시 저장 거부)

| # | 검사 | 예 |
|---|---|---|
| 1 | JSON Schema (`masters/schema/customer.schema.json`) | 필수 키 누락, 타입 오류 |
| 2 | **33필드 전량 선언** | 1개라도 빠지면 거부 |
| 3 | 코드 값 형식 | ZBRAND는 숫자 문자열, max_len 준수 |
| 4 | 참조 무결성 | `from: rule, rule: brand_code` 의 `brand_code`가 실제 존재하는가 |
| 5 | 분기표 정합성 | 도달 불가 행 / 중복 조건 |
| 6 | 순환 참조 | `extends` 루프 |

### 3.2 영향 미리보기 (권장, 저장 전)

거래처별 샘플 발주서(골든 픽스처)를 **현재 규칙 / 변경 규칙** 두 번 실행해 33필드 결과를 diff.

```
변경 영향:  12행 중 4행이 달라집니다

  행  필드     현재            변경 후
  3   ZBRAND   127             205      ⚠
  3   ZPKRE2   N,B12           N,B12    
  7   KUNNR2   319677          319678   ⚠
```

> 규칙 변경이 실제로 무엇을 바꾸는지 **눈으로 확인한 뒤** 저장한다.
> LLM 재호출 없이 캐시된 추출 결과(`storage/llm_cache/`)를 재사용하므로 **비용 0**.

### 3.3 변경 이력 · 롤백

```
masters/_history/{code}/{YYYYMMDD-HHMMSS}-{user}.yaml     저장 직전 스냅샷
storage/audit/masters/{YYYY-MM}.jsonl                      변경 감사 로그
```

- 이력 목록에서 **버전 간 diff**(변경 필드만 하이라이트)
- **되돌리기**: 과거 버전을 현재로 복원(복원도 새 스냅샷 생성 → 이력이 끊기지 않음)
- 보존: 최근 100버전 또는 2년

### 3.4 동시 편집 충돌

파일 기반이므로 **낙관적 잠금**을 쓴다. 조회 시 내려준 `etag`(파일 해시)를 저장 시 함께 보내고, 서버 파일 해시와 다르면 `409` → "다른 사용자가 먼저 저장했습니다. 새로고침 후 다시 시도하세요."

### 3.5 권한

| 역할 | 권한 |
|---|---|
| 조회자 | 읽기 전용 (현업 대부분) |
| 편집자 | 판별규칙 · 분기표 · 참조표 · 화면컬럼 |
| 관리자 | + 33필드 매핑, 신규 거래처, YAML 원문 편집, 롤백 |

1차(사내망 무인증)에서는 **화면 분리 + 감사 로그**만 두고, 2차 SSO 도입 시 역할을 연결한다.

---

## 4. rules.md 자동 생성

저장 시 전 거래처 마스터를 읽어 `rules.md`(현업용 설명서)를 **통째로 다시 쓴다.**

| 마스터 | → | rules.md 섹션 |
|---|---|---|
| `meta` | | §3 거래처 헤더, §4 비교표 |
| `rules.keyword_map / exact_map` | | "발주서 문구 → 코드" 표 |
| `tables.*` | | "조건 → 결과" 표 |
| `fields.*` (from: const) | | "항상 고정되는 값" 표 |
| `on_no_match.action` | | 🔴/🟡 안내 문구 |
| `fields.*.explain` | | 수식 설명 (예: `N,B12,C7`) |

- 파일 상단에 **`<!-- 이 문서는 자동 생성됩니다. 직접 수정하지 마세요. -->`** 삽입
- 사람이 쓴 문단(§5 체크리스트, §6 대처법, §8 FAQ)은 **`<!-- manual:start -->` ~ `<!-- manual:end -->`** 블록으로 보존

---

## 5. API

| Method | Path | 설명 |
|---|---|---|
| GET | `/api/admin/masters` | 목록 (수정일시·수정자 포함) |
| GET | `/api/admin/masters/{code}` | 편집용 구조 + `etag` |
| POST | `/api/admin/masters/{code}/validate` | 저장 없이 검증만 (실시간) |
| POST | `/api/admin/masters/{code}/preview` | 영향 미리보기 (전/후 diff) |
| PUT | `/api/admin/masters/{code}` | 저장 (`If-Match: etag`) |
| GET | `/api/admin/masters/{code}/history` | 이력 목록 |
| GET | `/api/admin/masters/{code}/history/{ver}/diff` | 버전 diff |
| POST | `/api/admin/masters/{code}/rollback/{ver}` | 되돌리기 |
| POST | `/api/admin/masters` | 신규 거래처 (템플릿 생성) |
| GET/PUT | `/api/admin/refs/{name}` | 참조표 CSV 조회/저장 |

검증 실패 응답 (`design.md` §8 표준 에러 형식 준수):

```json
{ "error": {
    "code": "MASTER_VALIDATION_FAILED",
    "message": "마스터 검증에 실패했습니다.",
    "detail": { "issues": [
      { "path": "fields.ZBRAND", "message": "브랜드 코드는 숫자여야 합니다: 'ABC'" },
      { "path": "tables.ship_to_routing.rows[2]", "message": "위 행에 흡수되어 도달할 수 없습니다" }
    ]},
    "recoverable": true, "action": "FIX_INPUT" } }
```

---

## 6. 모듈 배치

```
backend/app/
├── admin/
│   ├── routes_masters.py       # §5 API
│   ├── master_writer.py        # 스냅샷 → 원자적 쓰기 → 캐시 무효화
│   ├── master_validator.py     # §3.1 (CI 스크립트와 동일 코드 재사용)
│   ├── impact_preview.py       # §3.2 전/후 diff (llm_cache 재사용)
│   ├── history.py              # 스냅샷 · diff · 롤백
│   └── docs_generator.py       # §4 rules.md 생성
frontend/src/pages/admin/
├── MasterListPage.tsx
├── MasterEditPage.tsx
└── components/{RuleTableEditor, DecisionTableEditor, FieldMappingTable,
               RefTableEditor, ImpactPreviewModal, HistoryPanel}.tsx
masters/
├── schema/customer.schema.json
└── _history/{code}/*.yaml
```

> `master_validator.py`는 **CI 검증 스크립트(`scripts/validate_masters.py`)와 동일한 코드**를 쓴다.
> 화면 검증과 CI 검증이 갈라지면 "화면에선 통과했는데 배포가 깨지는" 사고가 난다.

---

## 7. DB 도입 판단 (재확인)

**현 시점 도입하지 않는다.** 파일 + 스냅샷으로 요구사항이 전부 충족된다.

| DB가 필요해지는 조건 | 현황 |
|---|---|
| 마스터 편집자 5명 이상 동시 작업 | 1~2명 예상 → 낙관적 잠금으로 충분 |
| 규칙 이력 검색/통계 요구 | 없음 |
| 참조표 10만 행 초과 | MSC 참조표 소규모 |
| 승인 워크플로(요청→결재→반영) 필요 | 현재 owner 구두 승인 |

거래처가 **50곳**으로 늘어도 파일 50개일 뿐이라 문제되지 않는다. 승인 워크플로 요구가 생기는 시점이 실질적 DB 승격 트리거다.

---

## 8. 미확정 항목

| # | 항목 | 확인 대상 |
|---|---|---|
| 1 | 관리 화면 접근 권한을 1차부터 나눌 것인가 | 업무 담당 |
| 2 | 규칙 변경에 **승인 절차**가 필요한가 (필요 시 draft/published 이중화) | 업무 담당 |
| 3 | 영향 미리보기용 **샘플 발주서**를 거래처별로 확보 가능한가 | 현업 |
| 4 | 참조표를 현업이 직접 수정할 것인가, 요청 접수 방식일 것인가 | 현업 (`rules.md` §7과 연동) |
