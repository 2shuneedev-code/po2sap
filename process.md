# PO2SAP 업무 프로세스 · 화면 정의서

> 거래처 선택 → 발주서 업로드 → Claude 추출 → 규칙엔진 → 통합 스프레드시트 검수 → EAI 전송 → SAP CBO 적재
>
> 버전 **v1.0** · 2026-09-11 · **낡은 v0.3 전면 재작성**
>
> ### 이 문서의 범위
> **업무 흐름 · 역할 · 화면 · 예외 처리.** 프론트엔드 개발자의 화면 명세서다.
>
> | 주제 | 단일 원천(SSOT) |
> |---|---|
> | API 요청/응답 · 필드 목록 · 상태값 | **`contracts/api-contract.md`** |
> | 전송 필드 스펙(개수·한글명·max_len) | **`masters/_base/sap_defaults.yaml`** → `GET /api/masters/fields` |
> | 규칙 스키마 | **`masters/SCHEMA.md`** |
> | 기술 스택 · 배포 | **`design.md`** |
>
> ⚠ **필드 개수를 코드나 문서에 박지 않는다.** 이 문서도 "전송 필드 전량"이라고만 쓴다.
> 개수는 `GET /api/masters/fields` 응답이 정한다 (현재 36).

---

## 0. 확정된 구조

```
[거래처]      [담당자]              [PO2SAP]              [EAI]        [SAP]
 발주서   →   ①거래처 선택      →  추출·검수·JSON    →   중계    →  CBO 적재
 (메일)       ②파일 업로드           │                                    │
                                     │                                    ▼
                            여기까지 본 시스템 범위               세일즈오더 생성
```

| 항목 | 확정 내용 |
|---|---|
| **업로드 방식** | **거래처를 먼저 선택** → 규칙 미리보기 확인 → 파일 업로드 |
| 파일 형식 | MSC=**HTM** · KL=**PDF** · YGJP=**PDF**. **업로드 차단하지 않음**(안내 문구만) |
| **검수 단위** | 한 배치 = **한 거래처 + 파일 N개**, 전 파일을 **한 그리드에 통합** |
| **오더 분할** ★ | **1 문서 → N 오더 가능.** MSC는 출하처(Shipment) 블록마다 별개 오더 |
| 분할 표현 | 화면은 **한 그리드 통합**. 행마다 `BSTKD` · `KUNNR2` 만 다르고 `_group` 으로 구분 |
| 전송 단위 | **전체 일괄 전송** (파일별 개별 전송 없음) |
| 전송 형태 | flat — 1행 = 1품목, **전송 필드 전량** |
| 페이로드 | **`{"rows": [...]}` 만** (batch_id·meta 없음) |
| 중복 전송 | 허용 (SAP에서 덮어씀) → 멱등키 불필요 |
| EAI 인증 | 불필요 (사내망) |
| 응답 | 적재 성공/실패만. 세일즈오더 번호 없음 |
| 승인 절차 | 없음. 검수자가 바로 전송 |
| 전송 후 수정 | SAP에서 직접 수정 |
| 이력 화면 | **제외** (감사 로그는 JSONL 파일로 남김) |
| 변경 발주 | 이번 범위 제외 |

---

## 1. To-Be 업무 프로세스

```
 ① 발주서 수신      거래처 이메일 → 담당자가 파일 저장
        ▼
 ② 거래처 선택 ★    MSC / KL / YGJP …
        │           → 해당 거래처의 자동 적용 규칙이 화면에 표시됨
        ▼
 ③ 파일 업로드      같은 거래처 발주서 여러 부 동시 업로드
        ▼
 ④ 자동 파싱        Claude 추출 → 규칙엔진이 SAP 코드 결정 (파일당 5~20초)
        ▼
 ⑤ 통합 검수 ★     여러 파일의 모든 품목을 한 스프레드시트에서 확인·수정
        ▼
 ⑥ 전체 전송       [전송] → 전 행을 한 번에 EAI로 POST
        ▼
 ⑦ 결과 확인       "전송 완료"
 ────────────── 여기까지 PO2SAP ──────────────
 ⑧ SAP 오더 생성   SAP에서 CBO 테이블 기준으로 세일즈오더 생성
 ⑨ 오더 확인       담당자가 SAP에서 확인 (오류 시 SAP에서 직접 수정)
```

### ⚠ 담당자가 반드시 알아야 할 것

**"전송 완료" = CBO 적재 완료이지, 세일즈오더가 생성된 것이 아니다.**
→ 완료 화면에 **"세일즈오더 생성은 SAP에서 확인해 주세요"** 상시 표시.

---

## 2. 역할과 책임

| 역할 | 책임 |
|---|---|
| **오더 담당자** | 거래처 선택 · 업로드 · **검수(최종 책임)** · 전송 · SAP에서 오더 확인 |
| **규칙 책임자**(거래처별 `meta.owner`) | 브랜드/출하처/고정값 규칙의 정확성 확인, 변경 요청 |
| **시스템 담당자** | 규칙 마스터(YAML) 반영, 파싱 정확도 개선, 장애 대응 |
| **EAI/SAP 담당자** | EAI 인터페이스, CBO 테이블, 세일즈오더 생성 |

---

## 3. 배치 상태

`contracts/api-contract.md §5` 의 `status` 와 **동일한 값이다.**

| 상태 | 의미 | 담당자 조치 |
|---|---|---|
| `PARSING` | 파싱 중 | 대기 (프론트 1.5초 폴링) |
| `NEEDS_REVIEW` | 🔴 오류 존재 | **수정 필요** |
| `READY` | 검수 통과 | **전송 가능** |
| `SENDING` | 전송 중 | 대기 |
| `SENT` | EAI 전송 완료 | **SAP에서 오더 확인** |
| `SEND_FAILED` | 전송 실패 | [재전송] (중복 전송 무해) |

> 파일 단위 상태(`PARSING`/`DONE`/`FAILED`)는 배치 안에서 파일별로 별도 표시.

---

## 4. 화면 구성

## 화면 1. 거래처 선택 + 업로드 ★

**거래처를 먼저 고르면, 그 거래처에 자동 적용되는 규칙이 바로 보인다.**

```
┌──────────────────────────────────────────────────────────────────────┐
│  PO2SAP                                                           ⚙  │
├──────────────────────────────────────────────────────────────────────┤
│  ① 거래처 선택                                                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐                │
│  │   MSC    │ │    KL    │ │   YGJP   │ │   ...    │                │
│  │  100249  │ │  107525  │ │   3200   │ │          │                │
│  │   HTM    │ │   PDF    │ │   PDF    │ │          │                │
│  └──────────┘ └━━━━━━━━━━┘ └──────────┘ └──────────┘                │
│                  선택됨                                               │
├──────────────────────────────────────────────────────────────────────┤
│  ② 이 거래처에 자동 적용되는 값                          [자세히 ▾]   │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  고정값                                                         │  │
│  │   판매처 KUNNR1  107525     브랜드   ZBRAND  2 (OEM)            │  │
│  │   운송수단 VSART  04         오더유형 AUART  ZEXP                │  │
│  │   판매조직 VKORG  1000                                          │  │
│  │                                                                 │  │
│  │  자동 판별 규칙                                                  │  │
│  │   발주서에 "WIDIA GTD" 포함  →  ZPKRE2 · EMPST = WGT            │  │
│  │   발주서에 "KENNAMETAL" 포함 →  ZPKRE2 · EMPST = KMT            │  │
│  │   ※ 위에서부터 순서대로 확인 (WIDIA GTD 우선)                    │  │
│  │                                                                 │  │
│  │  ⚠ 확인 중인 값: KUNNR2 · ZSHCO                                 │  │
│  │  나머지 항목은 발주서에서 읽어옵니다.                             │  │
│  └────────────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────────┤
│  ③ 발주서 업로드                                                      │
│      ┌────────────────────────────────────────────────┐              │
│      │   📄  KL 발주서를 여기에 끌어다 놓으세요         │              │
│      │        PDF · 여러 개 동시 가능                  │              │
│      │              [파일 선택]                        │              │
│      └────────────────────────────────────────────────┘              │
│                                                                      │
│   📄 4507628839.pdf     ✔ 파싱완료  12행                             │
│   📄 4507628840.pdf     ✔ 파싱완료   8행                             │
│   📄 4507628841.pdf     ⏳ 파싱중…                                    │
│                                                    [검수하기 ▶]      │
└──────────────────────────────────────────────────────────────────────┘
```

### 설계 포인트

| 항목 | 내용 |
|---|---|
| **거래처 선택 우선** | 자동판별 불필요 → **오판 리스크 원천 제거** |
| **규칙 미리보기** ★ | `GET /api/masters/customers/{code}/preview` 응답을 **모양 그대로** 렌더 |
| 렌더 방식 | `fixed[]` / `rules[]` / `split` / `todos[]` 4개 블록. **거래처별 분기 코드를 프론트에 넣지 않는다** |
| 아이콘 구분 | `rules[].kind` (`keyword_map` / `value_map` / `table` / `lookup`) 로만 구분 |
| `todos[]` | "⚠ 확인 중인 값" 으로 표시 — 미확정 값을 현업에 숨기지 않는다 |
| `split` | 분할이 있으면 "출하처별로 오더를 나눕니다" 안내 |
| 표시 원천 | **YAML 자동 생성** → 규칙이 바뀌면 화면도 자동 반영 (문서-실동작 불일치 없음) |
| 파일 형식 | `file_types` 는 **안내용.** 다른 형식이면 경고만 띄우고 업로드는 허용 |
| 파싱 | 업로드 즉시 파일별 병렬 시작 |
| 다음 단계 | 1개 이상 파싱 완료 시 **[검수하기]** 활성화 |
| 거래처 변경 | 업로드 후 변경하면 "기존 파일이 초기화됩니다" 확인 |

> **MSC 선택 시 표시 예** (`contracts/examples/preview_msc.json` 그대로)
> ```
> 출하처(Ship To) 분기
>   ELKHART    →  KUNNR2 100249 · ZPKRE2 기본값 C
>   HARRISBURG →  KUNNR2 319677 · ZPKRE2 기본값 N
>   RENO       →  KUNNR2 319678 · ZPKRE2 기본값 O
>   ATLANTA    →  KUNNR2 319679 · ZPKRE2 기본값 A
> 브랜드 판별 — HERTEL→38 · INTERSTATE→127 · ACCUPRO→205 · CLASS C→428
> 참조표 — 거래처 품번으로 조회해 ZPKRE2 비고에 덧붙입니다
> 오더 분할 — 출하처(Shipment)별로 오더를 나눕니다
> ```

---

## 화면 2. 통합 검수 (본체) ★

**여러 파일의 모든 품목을 한 스프레드시트에. 1행 = 1품목. 전송 필드 전 컬럼. 모든 셀 편집 가능.**

```
┌───────────────────────────────────────────────────────────────────────────────────────┐
│ ← 뒤로   MSC · 파일 2개 · 총 49행            ⛔오류 1  ⚠경고 3      [전체 전송 ▶]      │
├───────────────────────────────────────────────────────────────────────────────────────┤
│ [컬럼설정 ⚙] [열 일괄채우기] [되돌리기]  파일필터:[전체 ▾] 그룹:[전체 ▾]  49행·1,240 EA │
├────┬────────────────┬──────────┬───────────────┬────────┬──────────┬────────┬─────────┤
│ #  │ 파일           │ 그룹     │BSTKD          │KUNNR2  │MATNR     │KWMENG  │ZPKRE2   │
│    │ (화면 전용)    │(화면전용)│               │        │          │        │         │
├────┼────────────────┼──────────┼───────────────┼────────┼──────────┼────────┼─────────┤
│ 1  │ P7988114.HTM   │ELKHART   │7988114(ELKH…) │100249  │T1634585  │  30    │C        │
│ 2  │ P7988114.HTM   │ELKHART   │7988114(ELKH…) │100249  │🔴        │  12    │C        │
│ …  │                │          │               │        │          │        │         │
│ 21 │ P7988114.HTM   │RENO      │7988114(RENO)  │319678  │T1634590  │   6    │O        │
│ …  │                │          │               │        │          │        │         │
│ 40 │ P8033553.HTM   │ATLANTA   │8033553(ATLA…) │319679  │T1634601  │  15    │A,Blue R…│
└────┴────────────────┴──────────┴───────────────┴────────┴──────────┴────────┴─────────┘
                                                   ← 가로 스크롤로 전 컬럼 →
┌───────────────────────────────────────────────────────────────────────────────────────┐
│ ⛔ 2행 (P7988114.HTM) MATNR — 자재번호를 찾지 못했습니다          [해당 셀로 이동]      │
│ ⚠ 14행 (P7988114.HTM) ZPKRE2 — 참조표에 없는 품번입니다 (09876599)                    │
└───────────────────────────────────────────────────────────────────────────────────────┘
```

### 4.2.1 컬럼 구성

| 항목 | 설계 |
|---|---|
| 컬럼 | **`columns` 응답의 전 필드** + 화면 전용 3개(`#`, `파일`, `그룹`) |
| **화면 전용 컬럼** | `_` 접두 메타(`_file`, `_group`, `_line_no`) 기반. **전송 JSON에 포함되지 않음** |
| 고정(pin) | `grid.pinned` 응답값 + 화면 전용 컬럼 — 가로 스크롤해도 좌측 고정 |
| **컬럼 숨김** | `grid.hidden` 응답값. **숨겨도 전송 JSON에는 전 필드 포함**(빈값 `""`) |
| 컬럼 폭 | `grid.width` |
| 헤더 | SAP 필드명 + 한글명 2줄 (`MATNR` / `자재번호`) — 한글명은 `/api/masters/fields` |
| 파일 구분 | 파일이 바뀌는 행에 옅은 구분선. **파일 필터**로 특정 파일만 보기 |
| **그룹 구분** ★ | `_group`(출하처 등) 이 바뀌는 행에 구분선. **그룹 필터** 제공 |

> **왜 전 필드를 다 펼치는가**: 헤더성 필드도 **품목마다 값이 다를 수 있기 때문.**
> 상단 폼으로 분리하면 그 케이스를 표현할 수 없다.

### 4.2.2 편집 보조 기능

| 기능 | 조작 | 용도 |
|---|---|---|
| **열 일괄 채우기** | 컬럼 헤더 우클릭 → 값 입력 → 적용 범위(**전체 / 현재 파일 / 현재 그룹**) | 출하처·브랜드 대량 수정 |
| **아래로 채우기** | 셀 드래그 / `Ctrl+D` | 엑셀과 동일 |
| **엑셀 붙여넣기** | 범위 복사 → `Ctrl+V` | 엑셀로 준비한 값 대량 반영 |
| **되돌리기** | `Ctrl+Z` | 실수 복구 |
| **파싱값으로 복원** | 셀 우클릭 → "원래 값으로" | 잘못 고쳤을 때 |
| **행 삭제** | 행 우클릭 → 삭제 | 불필요한 품목 제외 |

> 편집 값은 **프론트가 주인**이다. 폴링/재검증 응답이 사용자 입력을 덮어쓰지 않는다
> (`POST /validate` 는 `issues`/`edited` 만 반환 — `contracts §6`).

### 4.2.3 검증 표시

| 표시 | 의미 | 전송 |
|---|---|---|
| 🔴 빨강 (`severity: error`) | 필수값 누락 / 규칙 매칭 실패 / 형식·길이 오류 / 합계 불일치 | **차단** |
| 🟡 노랑 (`severity: warn`) | 참조표 미등록, 낮은 파싱 신뢰도 | 가능 |
| 🔵 파랑 (`edited[]`) | 담당자가 직접 수정한 셀 | 가능 |

- 상단 카운터 · 셀 색상 · 하단 이슈 목록 **3중 표시**
- 이슈에 **파일명 + 행번호** 함께 표기 (여러 파일이 섞여 있으므로 필수)
- 이슈 클릭 → 해당 셀로 이동
- 2차: 셀 hover 근거 툴팁, 원문 대조 뷰

### 4.2.4 합계 바
`49행 · 1,240 EA · 9,240.00` — **파일별/그룹별 합계도 필터 선택 시 표시.**
각 파일의 발주서 기재 합계와 자동 대조하여 불일치 시 🔴(라인 누락 탐지).

---

## 화면 3. 전송 확인 → 결과 (모달)

```
┌─────────────────────────────────────┐      ┌──────────────────────────────────┐
│  전송 확인                           │      │  ✅ 전송 완료                     │
├─────────────────────────────────────┤      ├──────────────────────────────────┤
│  거래처   MSC                        │      │  49행이 EAI로 전송되었습니다.     │
│  파일     2개                        │      │                                  │
│   · P7988114.HTM   45행              │  →   │  ⚠ 세일즈오더 생성은             │
│   · P8033553.HTM    4행              │      │     SAP에서 확인해 주세요.        │
│  오더     4건 (출하처별)              │      │                                  │
│  총계     49행 / 1,240 EA            │      │  전송시각 2026-09-11 09:12       │
│                                     │      │                                  │
│  [전송할 JSON 보기 ▾]                │      │        [처음으로]                │
│                                     │      │                                  │
│          [취소]    [전송]            │      └──────────────────────────────────┘
└─────────────────────────────────────┘
```

- **전 파일의 행을 한 번에 전송** (파일별 개별 전송 없음)
- 🔴 오류가 하나라도 있으면 [전송] 비활성화 (서버도 **HTTP 409 `HAS_ERRORS`** 로 이중 차단)
- 실패 시 오류 메시지 + [재전송] (중복 전송 무해)

---

## 5. 예외 처리

| 상황 | 처리 |
|---|---|
| **파싱 실패** | 해당 파일만 `FAILED` 표시, 나머지는 정상 진행 |
| **파일 형식 불일치** | 안내만 ("MSC는 보통 HTM입니다") — **차단하지 않음** |
| **거래처 잘못 선택** | 뒤로 가서 변경 (기존 파일 초기화 확인) |
| **규칙 매칭 실패** | 🔴 셀에서 직접 입력/선택. 반복되면 규칙(YAML) 추가 요청 |
| **참조표 미등록 품번** | 🟡 경고 + 기본값 진행. 참조표 파일이 없어도 정상 동작 |
| **미확정 값(`todo`)** | 규칙 카드에 "확인 중" 표시 + 해당 필드는 검수에서 직접 입력 |
| **합계 불일치** | 🔴 "라인 누락 가능" → 원문 대조 |
| **EAI 전송 실패** | 3회 자동 재시도 → [재전송]. **중복 전송 무해** |
| **전송 후 오류 발견** | **SAP에서 직접 수정** |
| **SAP 오더 생성 실패** | SAP 담당자 확인 (본 시스템 범위 밖) |

---

## 6. MVP 범위

| 포함 ✅ | 제외 ❌ |
|---|---|
| 거래처 선택 + **규칙 미리보기 카드** | 거래처 자동 판별 |
| 다중 파일 업로드 (동일 거래처) | 여러 거래처 혼합 업로드 |
| Claude 추출 + 근거·합계 검증 | 스캔본 이미지 경로 |
| 규칙엔진 (고정값/키워드/참조표/**결정표**/**분할**) | 상속 체인, 커버리지 검사 |
| **통합 전 컬럼 그리드 + 편집 보조** | 근거 툴팁, 원문 대조 뷰 |
| 필수값·길이 검증 + 오류 표시 | — |
| **전체 일괄 전송** + 모의 EAI 서버 | 재시도 큐 |
| 전송 확인/결과 모달 | **이력 화면**, 마스터 관리 UI, 로그인 |

일정과 현재 진행 상태는 **`NEXT.md`** 가 관리한다.

### 성공 기준

| # | 기준 |
|---|---|
| 1 | 거래처 선택 시 **고정값·판별규칙·미확정값이 화면에 정확히 표시** |
| 2 | 발주서 여러 부 업로드 → 품목이 **누락 없이** 한 표에 통합 표시 |
| 3 | 브랜드/출하처/비고가 **규칙대로** 자동 입력 — `samples/MSC/*.csv`(기존 운영 결과지)와 일치 |
| 4 | MSC 1부가 **출하처 수만큼 오더로 분할**되고 `BSTKD`·`KUNNR2` 가 맞음 |
| 5 | 화면 수정값이 전송 JSON에 반영 |
| 6 | 전 행이 한 번에 EAI(또는 모의)로 전송되고 결과 표시 |
| 7 | KL 또는 YGJP가 **YAML 추가만으로** 동작 |
