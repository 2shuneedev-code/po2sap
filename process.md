# PO2SAP 업무 프로세스 · 화면 정의서

> 거래처 선택 → 발주서 업로드 → Claude 파싱 → 스프레드시트 통합 검수 → EAI(HTTPS/JSON) 전송 → SAP CBO 적재
> 버전 **v0.3** · 2026-09-09
>
> v0.2 → v0.3 확정: **거래처 선택 후 업로드** 방식, **여러 파일 통합 검수·일괄 전송**, 페이로드는 `rows` 배열만, 이력 화면 제외

---

## 0. 확정된 구조

```
[거래처]      [담당자]              [PO2SAP]              [EAI]        [SAP]
 발주서   →   ①거래처 선택      →  파싱·검수·JSON    →   중계    →  CBO 적재
 (메일)       ②파일 업로드           │                                    │
                                     │                                    ▼
                            여기까지 본 시스템 범위               세일즈오더 생성
```

### 0.1 확정 사항

| 항목 | 확정 내용 |
|---|---|
| **업로드 방식** | **거래처를 먼저 선택** → 해당 거래처 규칙 확인 → 파일 업로드 |
| **검수 단위** | **한 배치 = 한 거래처 + 파일 여러 개**, 전 파일을 **한 그리드에 통합** |
| **전송 단위** | **전체 일괄 전송** (파일별 개별 전송 없음) |
| 전송 형태 | flat — 1행 = 1품목, **33필드 전부** |
| 페이로드 | **`{"rows": [...]}` 만** (batch_id·meta 없음) |
| 중복 전송 | 허용 (SAP에서 덮어씀) → 멱등키 불필요 |
| EAI 인증 | 불필요 (사내망) |
| 응답 | 적재 성공/실패만. 세일즈오더 번호 없음 |
| 오더 그룹핑/분할 | 불필요 |
| 승인 절차 | 없음. 검수자가 바로 전송 |
| 전송 후 수정 | SAP에서 직접 수정 |
| 이력 화면 | **제외** (감사 로그는 파일로 남김) |
| 변경 발주 | 이번 범위 제외 |

### 0.2 SAP 전송 필드 — **33개**

```
AUART, VKORG, VTWEG, VBELN, KUNNR1, KUNNR2, KUNNR3, BSTKD, VDATU, ZTERM,
INCO1, INCO2, ZBRAND, ZSHCO, ZPKRE1, MATNR, MAKTX, KWMENG, LGORT, ETDAT,
BATCH, VALTY, ZPKRE2, EMPST, VSART, PRICE, WAERK, BSTDK_E, POSEX, DELCO,
BSTKD_E, AUGRU, VKAUS
```
> 이전 문서의 "34개"는 오기 (실제 33개).
> 헤더성 필드도 **품목마다 값이 다를 수 있으므로** 모든 행에 개별 컬럼으로 존재한다.

---

## 1. To-Be 업무 프로세스

```
 ① 발주서 수신      거래처 이메일 → 담당자가 파일 저장
        ▼
 ② 거래처 선택 ★    MSC / KL / YGJP …
        │           → 해당 거래처의 자동 적용 규칙이 화면에 표시됨
        ▼
 ③ 파일 업로드      같은 거래처 발주서 여러 부 동시 업로드
        ▼
 ④ 자동 파싱        Claude 추출 → 규칙엔진이 SAP 코드 결정 (파일당 5~20초)
        ▼
 ⑤ 통합 검수 ★     여러 파일의 모든 품목을 한 스프레드시트에서 확인·수정
        ▼
 ⑥ 전체 전송       [전송] → 전 행을 한 번에 EAI로 POST
        ▼
 ⑦ 결과 확인       "전송 완료"
 ────────────── 여기까지 PO2SAP ──────────────
 ⑧ SAP 오더 생성   SAP에서 CBO 테이블 기준으로 세일즈오더 생성
 ⑨ 오더 확인       담당자가 SAP에서 확인 (오류 시 SAP에서 직접 수정)
```

### ⚠ 담당자가 반드시 알아야 할 것

**"전송 완료" = CBO 적재 완료이지, 세일즈오더가 생성된 것이 아니다.**
→ 완료 화면에 **"세일즈오더 생성은 SAP에서 확인해 주세요"** 상시 표시.

---

## 2. 역할과 책임

| 역할 | 책임 |
|---|---|
| **오더 담당자** | 거래처 선택 · 업로드 · **검수(최종 책임)** · 전송 · SAP에서 오더 확인 |
| **규칙 책임자**(거래처별) | 브랜드/도착지/고정값 규칙 정확성 확인, 변경 요청 |
| **시스템 담당자** | 규칙 마스터 반영, 파싱 정확도 개선, 장애 대응 |
| **EAI/SAP 담당자** | EAI 인터페이스, CBO 테이블, 세일즈오더 생성 |

---

## 3. 배치 상태

| 상태 | 의미 | 담당자 조치 |
|---|---|---|
| `PARSING` | 파싱 중 | 대기 |
| `NEEDS_REVIEW` | 🔴 오류 존재 | **수정 필요** |
| `READY` | 검수 통과 | **전송 가능** |
| `SENDING` | 전송 중 | 대기 |
| `SENT` | EAI 전송 완료 | **SAP에서 오더 확인** |
| `SEND_FAILED` | 전송 실패 | [재전송] (중복 전송 무해) |

> 파일 단위 상태(파싱완료/실패)는 배치 안에서 파일별로 별도 표시.

---

## 4. 화면 구성

## 화면 1. 거래처 선택 + 업로드 ★

**거래처를 먼저 고르면, 그 거래처에 자동 적용되는 규칙이 바로 보인다.**

```
┌──────────────────────────────────────────────────────────────────────┐
│  PO2SAP                                                           ⚙  │
├──────────────────────────────────────────────────────────────────────┤
│  ① 거래처 선택                                                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐                │
│  │   MSC    │ │    KL    │ │   YGJP   │ │   ...    │                │
│  │  100249  │ │  107525  │ │   3200   │ │          │                │
│  │   PDF    │ │   HTM    │ │   PDF    │ │          │                │
│  └──────────┘ └━━━━━━━━━━┘ └──────────┘ └──────────┘                │
│                  선택됨                                               │
├──────────────────────────────────────────────────────────────────────┤
│  ② 이 거래처에 자동 적용되는 값                          [자세히 ▾]   │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  고정값                                                         │  │
│  │   판매처 KUNNR1  107525     브랜드   ZBRAND  2 (OEM)            │  │
│  │   운송수단 VSART  04         오더유형 AUART  ZOR                 │  │
│  │   판매조직 VKORG  1000       유통경로 VTWEG  10                  │  │
│  │                                                                 │  │
│  │  자동 판별 규칙                                                  │  │
│  │   발주서에 "WIDIA GTD" 포함  →  ZPKRE2 · EMPST = WGT            │  │
│  │   발주서에 "KENNAMETAL" 포함 →  ZPKRE2 · EMPST = KMT            │  │
│  │   ※ 위에서부터 순서대로 확인 (WIDIA GTD 우선)                    │  │
│  │                                                                 │  │
│  │  나머지 항목은 발주서에서 읽어옵니다.                             │  │
│  └────────────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────────┤
│  ③ 발주서 업로드                                                      │
│      ┌────────────────────────────────────────────────┐              │
│      │   📄  KL 발주서를 여기에 끌어다 놓으세요         │              │
│      │        HTM · 여러 개 동시 가능                  │              │
│      │              [파일 선택]                        │              │
│      └────────────────────────────────────────────────┘              │
│                                                                      │
│   📄 KL_20260909_001.htm    ✔ 파싱완료  12행                         │
│   📄 KL_20260909_002.htm    ✔ 파싱완료   8행                         │
│   📄 KL_20260909_003.htm    ⏳ 파싱중…                                │
│                                                    [검수하기 ▶]      │
└──────────────────────────────────────────────────────────────────────┘
```

### 설계 포인트

| 항목 | 내용 |
|---|---|
| **거래처 선택 우선** | 자동판별 불필요 → **오판 리스크 원천 제거**. 파일 형식도 거래처별로 안내(PDF/HTM) |
| **규칙 미리보기** ★ | 선택 즉시 그 거래처의 **고정값 + 자동 판별 규칙**을 표시. 현업이 "무엇이 자동으로 채워지는지" 업로드 전에 확인 |
| 규칙 표시 내용 | ① 고정값 목록 ② 키워드 판별 규칙 ③ 분기표(Ship To 등) ④ 참조표 사용 여부 |
| 표시 원천 | **거래처 설정 파일(YAML)에서 자동 생성** → 규칙이 바뀌면 화면도 자동 반영 (문서-실동작 불일치 없음) |
| 업로드 | 같은 거래처 파일만 다중 업로드. 형식이 안 맞으면 즉시 경고 |
| 파싱 | 업로드 즉시 파일별 병렬 시작 |
| 다음 단계 | 파일이 1개 이상 파싱 완료되면 **[검수하기]** 활성화 |
| 거래처 변경 | 업로드 후 거래처를 바꾸면 "기존 파일이 초기화됩니다" 확인 후 전환 |

> **MSC 선택 시 추가 표시 예**
> ```
> 분기 규칙 — 출하처(Ship To)
>   Ship To에 ELKHART    →  KUNNR2 100249 · ZPKRE2 C
>   Ship To에 HARRISBURG →  KUNNR2 319677 · ZPKRE2 N
>   Ship To에 RENO       →  KUNNR2 319678 · ZPKRE2 O
>   Ship To에 ATLANTA    →  KUNNR2 319679 · ZPKRE2 A
> 브랜드 판별 — HERTEL→38 · INTERSTATE→127 · ACCUPRO→205 · CLASS C→428
> 참조표 — 거래처 품번으로 B/C코드를 조회해 ZPKRE2에 추가합니다
> ```

---

## 화면 2. 통합 검수 (본체) ★

**여러 파일의 모든 품목을 한 스프레드시트에 펼친다. 1행 = 1품목. 33컬럼 전부. 모든 셀 편집 가능.**

```
┌───────────────────────────────────────────────────────────────────────────────────────┐
│ ← 뒤로   KL · 파일 3개 · 총 32행              ⛔오류 1  ⚠경고 2      [전체 전송 ▶]     │
├───────────────────────────────────────────────────────────────────────────────────────┤
│ [컬럼설정 ⚙] [열 일괄채우기] [되돌리기]  파일필터: [전체 ▾]      32행 · 810 EA · 9,240 │
├────┬──────────────────┬───────┬────────────┬──────────────┬────────┬───────┬─────────┤
│ #  │ 파일             │POSEX  │MATNR       │MAKTX         │KWMENG  │PRICE  │BSTKD    │
│    │ (화면 전용)      │       │            │              │        │       │         │
├────┼──────────────────┼───────┼────────────┼──────────────┼────────┼───────┼─────────┤
│ 1  │ KL_…_001.htm     │000010 │1234567890  │END MILL 10MM │  25.000│  12.35│PO-4471…│
│ 2  │ KL_…_001.htm     │000020 │🔴          │DRILL 8MM     │  10.000│   8.20│PO-4471…│
│ …  │                  │       │            │              │        │       │         │
│ 13 │ KL_…_002.htm     │000010 │2234567890  │TAP M6        │   5.000│  15.00│PO-4471…│
│ …  │                  │       │            │              │        │       │         │
│ 21 │ KL_…_003.htm     │000010 │3234567890  │REAMER 12MM   │  40.000│  22.10│PO-4472…│
└────┴──────────────────┴───────┴────────────┴──────────────┴────────┴───────┴─────────┘
                                                       ← 가로 스크롤로 33컬럼 전부 →
┌───────────────────────────────────────────────────────────────────────────────────────┐
│ ⛔ 2행 (KL_…_001.htm) MATNR — 자재번호를 찾지 못했습니다          [해당 셀로 이동]      │
│ ⚠ 14행 (KL_…_002.htm) ZPKRE2 — 참조표에 없는 품번입니다 (09876599)                    │
└───────────────────────────────────────────────────────────────────────────────────────┘
```

### 4.2.1 컬럼 구성

| 항목 | 설계 |
|---|---|
| 컬럼 | **33개 전부** + 화면 전용 컬럼 2개(`#`, `파일`) |
| **화면 전용 컬럼** | `#`(행번호), `파일`(출처 파일명) — **전송 JSON에는 포함되지 않음** |
| 고정(pin) | `#`, `파일`, `POSEX`, `MATNR`, `MAKTX` — 가로 스크롤해도 좌측 고정 |
| 컬럼 순서 | 품목 핵심 → 오더 정보 → 코드/분류 → 시스템값 |
| **컬럼 숨김** | 거래처별 설정(`grid.hidden`). **숨겨도 전송 JSON에는 33개 전부 포함**(빈값) |
| 헤더 | SAP 필드명 + 한글명 2줄 (`MATNR` / `자재번호`) |
| 파일 구분 | 파일이 바뀌는 행에 옅은 구분선. **파일 필터**로 특정 파일만 보기 가능 |

> **왜 33개를 다 펼치는가**: 헤더성 필드도 **품목마다 값이 다를 수 있기 때문.** 상단 폼으로 분리하면 그 케이스를 표현할 수 없다.

### 4.2.2 편집 보조 기능

| 기능 | 조작 | 용도 |
|---|---|---|
| **열 일괄 채우기** | 컬럼 헤더 우클릭 → 값 입력 → 적용 범위 선택(**전체 행 / 현재 파일 행만**) | 도착지·브랜드 등 대량 수정 |
| **아래로 채우기** | 셀 드래그 / `Ctrl+D` | 엑셀과 동일 |
| **엑셀 붙여넣기** | 범위 복사 → `Ctrl+V` | 엑셀로 준비한 값 대량 반영 |
| **되돌리기** | `Ctrl+Z` | 실수 복구 |
| **파싱값으로 복원** | 셀 우클릭 → "원래 값으로" | 잘못 고쳤을 때 |
| **행 삭제** | 행 우클릭 → 삭제 | 불필요한 품목 제외 |

> 열 일괄 채우기는 **파일이 여러 개이므로 적용 범위를 물어본다.** (전체 / 이 파일만)

### 4.2.3 검증 표시

| 표시 | 의미 | 전송 |
|---|---|---|
| 🔴 빨강 | 필수값 누락 / 규칙 매칭 실패 / 형식 오류 | **차단** |
| 🟡 노랑 | 참조표 미등록, 낮은 파싱 신뢰도 | 가능 |
| 🔵 파랑 | 담당자가 직접 수정한 셀 | 가능 |

- 상단 카운터 · 셀 색상 · 하단 이슈 목록 **3중 표시**
- 이슈에 **파일명 함께 표기** (여러 파일이 섞여 있으므로 필수)
- 이슈 클릭 → 해당 셀로 이동
- 2차: 셀 hover 근거 툴팁, 원문 PDF 대조 뷰

### 4.2.4 합계 바
`32행 · 810 EA · 9,240.00` (전체) — **파일별 합계도 파일 필터 선택 시 표시.**
각 파일의 발주서 기재 합계와 자동 대조하여 불일치 시 🔴(라인 누락 탐지).

---

## 화면 3. 전송 확인 → 결과 (모달)

```
┌─────────────────────────────────────┐      ┌──────────────────────────────────┐
│  전송 확인                           │      │  ✅ 전송 완료                     │
├─────────────────────────────────────┤      ├──────────────────────────────────┤
│  거래처   KL                         │      │  32행이 EAI로 전송되었습니다.     │
│  파일     3개                        │      │                                  │
│   · KL_…_001.htm   12행              │  →   │  ⚠ 세일즈오더 생성은             │
│   · KL_…_002.htm    8행              │      │     SAP에서 확인해 주세요.        │
│   · KL_…_003.htm   12행              │      │                                  │
│  총계     32행 / 810 EA              │      │  전송시각 2026-09-09 09:12       │
│                                     │      │                                  │
│  [전송할 JSON 보기 ▾]                │      │        [처음으로]                │
│                                     │      │                                  │
│          [취소]    [전송]            │      └──────────────────────────────────┘
└─────────────────────────────────────┘
```

- **전 파일의 행을 한 번에 전송** (파일별 개별 전송 없음)
- 🔴 오류가 하나라도 있으면 [전송] 비활성화
- 실패 시 오류 메시지 + [재전송] (중복 전송 무해)

---

## 5. EAI 인터페이스 (확정)

### 5.1 요청

```
POST {EAI_ENDPOINT}
Content-Type: application/json; charset=utf-8
```

```json
{
  "rows": [
    {
      "AUART": "ZOR", "VKORG": "1000", "VTWEG": "10", "VBELN": "",
      "KUNNR1": "107525", "KUNNR2": "319677", "KUNNR3": "",
      "BSTKD": "PO-4471902", "VDATU": "20260920", "ZTERM": "NT30",
      "INCO1": "FCA", "INCO2": "INCHEON",
      "ZBRAND": "2", "ZSHCO": "A", "ZPKRE1": "",
      "MATNR": "1234567890", "MAKTX": "END MILL 10MM 4FL",
      "KWMENG": "25.000", "LGORT": "", "ETDAT": "20260915",
      "BATCH": "", "VALTY": "", "ZPKRE2": "WGT", "EMPST": "WGT",
      "VSART": "04", "PRICE": "12.35", "WAERK": "USD",
      "BSTDK_E": "20260901", "POSEX": "000010", "DELCO": "",
      "BSTKD_E": "PO-4471902", "AUGRU": "", "VKAUS": ""
    },
    { "...": "다음 품목 (다른 파일의 행도 같은 배열에 포함)" }
  ]
}
```

| 규약 | 내용 |
|---|---|
| 구조 | **`rows` 배열만.** 1행 = 1품목, **33필드 전부 포함** |
| 파일 구분 | **포함하지 않음** (화면 전용). 발주번호 `BSTKD`로 구분됨 |
| 날짜 | `"YYYYMMDD"` 문자열 |
| 수량/금액 | **문자열 decimal** (`"25.000"`) — 부동소수 오차 방지 |
| 코드값 | 문자열, 앞자리 0 보존 (`"04"`, `"000010"`) |
| 빈 값 | `""` (null 미사용) |
| 인코딩 | UTF-8 |

> 최상위를 배열(`[ {...}, {...} ]`)로 보내는 것도 설정(`PAYLOAD_ROOT=rows|array`)으로 전환 가능. EAI 편한 쪽으로 맞춘다.

### 5.2 응답

```json
{ "status": "OK", "message": "32건 적재 완료" }
```

| 응답 | 동작 |
|---|---|
| 2xx + `OK` | `SENT` 처리, 완료 화면 |
| 2xx + `ERROR` / 4xx | 오류 메시지 표시, 재전송 가능 |
| 5xx / 타임아웃 | 3회 자동 재시도 → 실패 시 [재전송] |

### 5.3 설정 (.env)

```bash
EAI_ENDPOINT=https://eai.corp.local/api/po2sap/salesorder
EAI_AUTH_MODE=none            # none | apikey | basic
EAI_TIMEOUT_SEC=60
PAYLOAD_ROOT=rows             # rows | array
```

---

## 6. 예외 처리

| 상황 | 처리 |
|---|---|
| **파싱 실패** | 해당 파일만 실패 표시, 나머지는 정상 진행. 원문 텍스트 뷰에서 수기 입력 가능 |
| **파일 형식 불일치** | 업로드 시 즉시 경고 ("KL은 HTM 파일입니다") |
| **거래처 잘못 선택** | 뒤로 가서 변경 (기존 파일 초기화 확인) |
| **규칙 매칭 실패** | 🔴 셀에서 드롭다운 선택. 반복되면 규칙 추가 요청 |
| **참조표 미등록 품번** | 🟡 경고 + 기본값 진행. 신규 품번이면 참조표 등록 요청 |
| **합계 불일치** | 🔴 "라인 누락 가능" → 원문 대조 |
| **EAI 전송 실패** | 3회 자동 재시도 → [재전송]. **중복 전송 무해** |
| **전송 후 오류 발견** | **SAP에서 직접 수정** |
| **SAP 오더 생성 실패** | SAP 담당자 확인 (본 시스템 범위 밖) |

---

## 7. 1주 MVP 계획

### 7.1 목표
**거래처를 고르고 → 발주서 여러 부를 올려 → 한 스프레드시트에서 검수하고 → JSON이 EAI로 전송되는 것을 눈으로 확인.**

### 7.2 범위

| 포함 ✅ | 제외 ❌ |
|---|---|
| 거래처 선택 + **규칙 미리보기 카드** | 자동 판별 |
| 다중 파일 업로드 (동일 거래처) | 여러 거래처 혼합 업로드 |
| Claude 추출 + 합계 검증 | 스캔본 이미지 경로 |
| 규칙엔진 (고정값/키워드/참조표/**결정표**) | 상속, 커버리지 검사 |
| **통합 33컬럼 그리드 + 편집 보조** | 근거 툴팁, 원문 대조 뷰 |
| 필수값 검증 + 오류 표시 | 전 필드 길이 검증 |
| **전체 일괄 전송** + 모의 EAI 서버 | 재시도 큐 |
| 전송 확인/결과 모달 | **이력 화면**, 마스터 조회 UI, 로그인 |

### 7.3 일정

| Day | 작업 | 확인 결과 |
|---|---|---|
| **D1** | 백엔드 스켈레톤, PDF/HTM 전처리, Claude 추출(구조화 출력) | 콘솔에서 파싱 JSON |
| **D2** | 규칙엔진 + MSC YAML + 33필드 행 생성 + 검증 | 33컬럼 데이터 생성 |
| **D3** | 프론트: 거래처 선택·규칙카드·업로드 + 통합 그리드 | 화면에서 편집 가능 |
| **D4** | 전체 전송 + 모의 EAI 서버 + 확인/결과 모달 | **엔드투엔드 관통** |
| **D5** | KL·YGJP **YAML만 추가**, 다중 파일 테스트, 데모 | 확장성 입증 |

> **D5가 이번 주 최대 성과**: KL/YGJP가 코드 수정 없이 설정만으로 붙으면 나머지 거래처 확장 일정이 계산 가능해진다.

### 7.4 착수 전제

```
□ Claude API 키 (개발용 즉시 발급) ← 없으면 D1부터 막힘
□ 샘플 발주서 (MSC 2~3부, 가능하면 KL/YGJP도)
□ MSC_REF.xlsx (참조표)
□ SAP 공통 고정값: AUART, VKORG, VTWEG, LGORT, ZTERM, INCO1/2  ← 임시값 시작 가능
□ EAI 엔드포인트 URL (없으면 모의 서버로 진행)
□ 실제 발주서 외부 API 전송 내부 승인 (또는 마스킹 샘플)
```

### 7.5 성공 기준

| # | 기준 |
|---|---|
| 1 | 거래처 선택 시 **고정값·판별규칙이 화면에 정확히 표시** |
| 2 | 발주서 여러 부 업로드 → 품목이 **누락 없이** 한 표에 통합 표시 |
| 3 | 브랜드/도착지/포장구분이 **규칙대로** 자동 입력 (기존 Excel 결과와 일치) |
| 4 | 화면 수정값이 전송 JSON에 반영 |
| 5 | 전 행이 한 번에 EAI(또는 모의)로 전송되고 결과 표시 |
| 6 | KL 또는 YGJP가 **YAML 추가만으로** 동작 |

---

## 8. 남은 확인 사항

| # | 항목 | 시점 |
|---|---|---|
| 1 | EAI 엔드포인트 URL / 최상위 형태(`rows` vs 배열) | D4 전 |
| 2 | SAP 공통 고정값 (AUART/VKORG/VTWEG/LGORT/ZTERM/INCO1/2) | D2 (임시값 가능) |
| 3 | 확장 거래처 명단·우선순위 (약 20곳) | 2차 |
| 4 | YGJP 브랜드 448/449/450 원문 구분 키 | YGJP 적용 시 |
| 5 | 거래처별 화면에서 숨길 컬럼 | 운영 후 조정 |
| 6 | 사내 서버 인터넷 아웃바운드 허용 | 이관 시점 |
| 7 | 변경 발주(Change PO) | 범위 제외 |
